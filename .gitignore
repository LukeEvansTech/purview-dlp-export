# PowerShell
*.user
*.tmp
*.bak

# Pester
TestResults*.xml
coverage*.xml

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Local smoke-test output directory — contains real tenant data, never commit
out-smoke/

# Local baseline outputs (these should NEVER be committed to the tool repo;
# they belong with the engagement workspace, not the tool)
baseline-*.json
baseline-*.meta.json
baseline-*.md
baseline-*.csv

# Allow the published example (not a real output — it is a test-fixture copy)
!examples/baseline-sample-overview.md
!examples/baseline-sample-detail.md
!examples/baseline-sample-matrix.csv
